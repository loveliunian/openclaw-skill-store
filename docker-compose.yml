services:
  app:
    build: .
    container_name: openclaw-skill-store
    restart: always
    ports:
      - "5050:5050"
    environment:
      - FLASK_ENV=production
      - DATABASE=/app/data/skills.db
    volumes:
      - skill-data:/app/data

volumes:
  skill-data:
